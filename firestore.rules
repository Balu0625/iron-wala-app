rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This rule allows a user to be created in the 'users' collection
    // only if they are logged in and the document ID matches their user ID.
    match /users/{userId} {
      allow create: if request.auth != null && request.auth.uid == userId;
    }
  }
}
